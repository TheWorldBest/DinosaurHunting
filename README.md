# 阿里笔试题：树桩围恐龙
已知每个树桩的坐标和恐龙的坐标，判断通过树桩是否可将恐龙围住。  
输入：  
输入数字包含1行，共2N个浮点数据，前面2（N-1）为树桩坐标，最后2个为恐龙出现的坐标，N=[2, 101]；  
输出：  
如果通过树桩能围住恐龙，则返回true，否则返回false；  

思路：  
先寻找所有点（N个，包括恐龙坐标）的最小外接凸多边形，然后判断恐龙坐标是否为这个凸多边形的一个顶点坐标，如果是，则不能围住，返回false，否则返回true